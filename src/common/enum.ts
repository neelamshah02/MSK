export enum ViewState {
  GRID,
  LIST
}

export enum SortingDirection {
  NONE,
  ASC,
  DESC
}

export enum AlertType {
  None,
  Success,
  Warning,
  Error
}

export enum Size {
  S,
  M,
  L
}

export enum Role {
  EducationCenter,
  VesselOwner,
  Assessor,
  ManningCompany,
  Admin
}
